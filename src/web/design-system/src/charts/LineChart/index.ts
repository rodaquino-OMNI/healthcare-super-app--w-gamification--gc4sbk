/**
 * @file LineChart component public API
 * 
 * This file serves as the entry point for the LineChart component,
 * exporting both the component and its TypeScript interface.
 */

import LineChart, { LineChartProps } from './LineChart';

export { LineChartProps };
export { LineChart };
export default LineChart;
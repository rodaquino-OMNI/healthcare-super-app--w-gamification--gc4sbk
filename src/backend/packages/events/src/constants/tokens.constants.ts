/**
 * Injection tokens for the events module.
 */

/**
 * Token for injecting Kafka module options.
 */
export const KAFKA_MODULE_OPTIONS = 'KAFKA_MODULE_OPTIONS';

/**
 * Token for injecting the schema registry service.
 */
export const EVENT_SCHEMA_REGISTRY = 'EVENT_SCHEMA_REGISTRY';

/**
 * Token for injecting Kafka client instance.
 */
export const KAFKA_CLIENT = 'KAFKA_CLIENT';

/**
 * Token for injecting Kafka producer instance.
 */
export const KAFKA_PRODUCER = 'KAFKA_PRODUCER';

/**
 * Token for injecting Kafka consumer factory.
 */
export const KAFKA_CONSUMER_FACTORY = 'KAFKA_CONSUMER_FACTORY';